spring:
  profiles:
    active: development
  application:
    name: sales-order-service
  cloud:
    config:
      uri: http://localhost:5055

kafka:
  bootstrap-servers: localhost:9092
  consumer:
    group-id: myGroup
