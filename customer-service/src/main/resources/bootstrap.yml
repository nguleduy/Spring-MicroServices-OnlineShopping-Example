spring:
  profiles:
    active: development
  application:
    name: customer-service
  cloud:
    config:
      uri: http://localhost:5055

management:
  endpoints:
    web:
      exposure:
        include: '*'
