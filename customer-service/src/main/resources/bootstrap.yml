spring:
  profiles:
    active: development
  application:
    name: customer-service
  cloud:
    config:
      uri: http://localhost:8088

management:
  endpoints:
    web:
      exposure:
        include: '*'
